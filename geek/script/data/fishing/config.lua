require "data.fishing.fish"
require "data.fishing.special"
require "data.fishing.system"
require "data.fishing.scene"
require "data.fishing.cannon_set"
require "data.fishing.bullet_set"
require "data.fishing.path"
require "data.fishing.troop_set"
require "data.fishing.bounding_box"

room_cfg = {
    weight = 0,
}

blacklist_cfg = {
    base_weight = -100,
    sub_weight_threshold = 400,
}

broadcast_cfg = {
    money = 10000,
    multiple = 100,
}